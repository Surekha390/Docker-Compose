We have to install docker compose
Give: docker compose up 
then containers will created and we should give http://localhost:8080 in the browser to see the output

Give -d when the containers executes and shut off immediately like:
docker compose up -d
